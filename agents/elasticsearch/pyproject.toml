[project]
name = "elasticsearch-agent"
version = "0.1.10"
description = "Agent for monitoring Elasticsearch clusters (REST + ECK CRs on Kubernetes)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "orrery-core",
    "requests>=2.34.2",
    "kubernetes>=36.0.2",
]

[tool.uv.sources]
orrery-core = { workspace = true }
